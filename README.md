# SoftwareEngineering

Added Two test case for now..

To add test cases just add in the main.cpp file, that would be enough if you are willing to use same satellite data.


Results are in results.txt

Each operation is run for 10000 times.
Arrayed implementation is 100 times better than adjacency list and matrix implmentation.